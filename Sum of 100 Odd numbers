#include <iostream>

using namespace std;
int main(){
    int i=0, num=0, sum=0;
 
 while (i < 100)
 {
  if(num%2 != 0)
  {
   sum = sum + num;
   i++;
  }
   num++;
 }
  cout << "The sum of first 100 odd numbers is " << sum;
 
    return 0;
}

